// ============================================
// 背景模糊 — 已禁用
// 极简版不使用模糊效果以降低 GPU 开销
// ============================================
// blur 配置块已全部移除。
// 如需重新启用，参考原始配置:
//   blur { passes 1; offset 1; noise 0.01; saturation 1.2; }
