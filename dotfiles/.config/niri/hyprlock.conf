# ============================================
# Hyprlock 配置文件 — 已弃用
# 极简版使用 swaylock 而非 hyprlock
#
# swaylock 使用方式:
#   swaylock -f -c 1e1e2e           # 纯色背景
#   swaylock -f -i ~/wallpaper.jpg  # 图片背景
# ============================================
